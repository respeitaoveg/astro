---
export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
const { default: innerHTML } = await import(`/src/assets/images/${icon}.svg?raw`);
---

<Fragment set:html={innerHTML} />
